{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Rebuild",
            "type": "shell",
            "command": [
                "mkdir -p debug &&",
                "cd debug &&",
                "cmake -DCMAKE_BUILD_TYPE=Debug .. &&",
                "cmake --build ."
            ]
        }
    ]
}